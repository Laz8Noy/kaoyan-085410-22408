# 08 推荐器网页

三个 HTML 是同一推荐器的迭代：

1. `kaoyan-recommendation-enhanced.html` — **建议用这个**  
2. `kaoyan-recommendation-merged.html`  
3. `kaoyan-recommendation.html` — 早期版  

`assets/` 与 `_shared/js/echarts.min.js` 是图表依赖。终极版择校网页里也内嵌了推荐器，功能以 `04-终极版择校` 的 20260826 HTML 更全。
